# Rule Generator

Portfolio tab: `Python`

Source: screenshot-reconstructed from the portfolio image. The original
screenshot is preserved in `screenshots/rule-generator.png`.

`rule-generator-V1.py` preserves the screenshot-style implementation that
prints the generated rules incrementally.

`rule-generator-V2.py` is a cleaned-up version that builds and returns the
same Curious George-style `title:` rules from cartesian products of keyword
variants.
